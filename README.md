
---
This is a project that can call an LLM to talk to it responsivly. 

## 🚀 Getting Started (Step-by-Step)

### 1. Prerequisites
- Install **Python 3.10+** (`python --version`)
- Install **Node.js 18+** (`node --version`)
- A code editor (VSCode recommended)

### 2. Get your OpenRouter API Key
1. Go to [OpenRouter Keys](https://openrouter.ai/keys) and sign up.
2. Click **"Create Key"** (they give free credits to start).
3. Copy the key (it starts with `sk-or-v1-...`).

### 3. Clone / Setup the Project
```bash
 You can do this by downloading the zip file.